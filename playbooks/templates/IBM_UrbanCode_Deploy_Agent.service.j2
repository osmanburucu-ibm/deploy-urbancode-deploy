
[Unit]
Description = IBM UrbanCode Deploy Agent
After = network.target network.service

[Service]
ExecStart = {{ installation_location }}/bin/agent start
ExecStop = {{ installation_location }}/bin/agent stop
Type = forking
GuessMainPID=no
Restart = always
RestartSec = 10
TimeoutSec = 10

[Install]
WantedBy = multi-user.target
